import { createContext } from "react";

export const maxPrice = createContext({});
export const minPrice = createContext({});
export const aspects = createContext({});
export const carUse = createContext({});
export const fuelType = createContext({});
export const transmissiontype = createContext({});
export const trunkSpace = createContext({});
export const passengerNumber = createContext({});
export const features = createContext({});
export const bodyType = createContext({});
export const carBrands = createContext({});
export const homeState = createContext({});
export const helperState = createContext({});
export const searchState = createContext({});